# Face Recognition Attendance System (FAISS)

A real-time attendance system using MTCNN for face detection, InceptionResnetV1 for embeddings, and FAISS for fast similarity search.

## Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt